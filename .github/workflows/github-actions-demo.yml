name: Checkov
on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Checkov Github Action
        id: checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: cdk.out
          soft_fail: true
          external_checks_repos: MaxSloof-Deloitte/checkov_custom_policy/custom_policies@main
          skip_check: CKV_AWS_54
